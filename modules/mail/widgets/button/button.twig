{% if wide %}
{{ void(outerTable.begin()) }}
{% endif %}
<tr>
    <td style="font-family: Helvetica, arial, sans-serif; font-size: 14px; color: #000000; text-align:left; line-height: 24px;"
        class="padding-right15">
        <table class="button">
            <tr>
                <td style="padding: 0 0 !important;">
                    <a href="{{ url }}" style="text-decoration: none;  width:100%; display:inline-block; padding: 8px 0 !important;">{{ content }}</a>
                </td>
            </tr>
        </table>
    </td>
</tr>
{% if wide %}
    {{ void(outerTable.end()) }}
{% endif %}