{{ void(table.begin({bgColor: '#EEEEEE'})) }}
    {{ void(textblock.begin()) }}
        <strong>Kundekvittering</strong>
    {{ void(textblock.end()) }}

    {{ space.widget({height: 15}) | raw}}

    {{ void(textblock.begin()) }}
        Bekæftelseskode: <a href="{{ vm.receiptUrl }}">{{ vm.bookingId }}</a>
    {{ void(textblock.end()) }}
    {{ void(textblock.begin()) }}
        {{ now() }}
    {{ void(textblock.end()) }}

    {{ space.widget({height: 20}) | raw }}

{{ void(table.end()) }}

{{ void(table.begin({bgColor: '#FFFFFF', borderSize: 1, borderBottom: false})) }}
    {{ void(textblock.begin()) }}
        <table width="100%">
            <tr>
                {{ void(textblock.begin({row:false, width:'auto', align:'left'})) }}
                Lejer
                {{ void(textblock.end()) }}
                {{ void(textblock.begin({row:false, width:'auto', align:'right'})) }}
                {{ vm.profileName }}
                {{ void(textblock.end()) }}
            </tr>
        </table>
    {{ void(textblock.end()) }}
{{ void(table.end()) }}

{{ void(table.begin({bgColor: '#FFFFFF', borderSize: 1, borderBottom: false})) }}
    {{ void(textblock.begin()) }}
        <table width="100%">
            <tr>
                {{ void(textblock.begin({row:false, width:'auto', align:'left'})) }}
                Udlejers adresse
                {{ void(textblock.end()) }}
                {{ void(textblock.begin({row:false, width:'auto', align:'right'})) }}
                {{ vm.bookingLocation }}
                {{ void(textblock.end()) }}
            </tr>
        </table>
    {{ void(textblock.end()) }}
{{ void(table.end()) }}

{{ void(table.begin({bgColor: '#FFFFFF', borderSize: 1, borderBottom: false})) }}
    {{ void(textblock.begin()) }}
    <table width="100%">
        <tr>
            {{ void(textblock.begin({row:false, width:'auto', align:'left'})) }}
            Udstyr
            {{ void(textblock.end()) }}
            {{ void(textblock.begin({row:false, width:'auto', align:'right'})) }}
            {{ vm.itemName }}
            {{ void(textblock.end()) }}
        </tr>
    </table>
    {{ void(textblock.end()) }}
{{ void(table.end()) }}

{{ void(table.begin({bgColor: '#FFFFFF', borderSize: 1, borderBottom: true})) }}
    {{ void(textblock.begin()) }}
    <table width="100%">
        <tr>
            {{ void(textblock.begin({row:false, width:'auto', align:'left'})) }}
            Længe på lejeperiode
            {{ void(textblock.end()) }}
            {{ void(textblock.begin({row:false, width:'auto', align:'right'})) }}
            {{ vm.rentingDays }} døgn
            {{ void(textblock.end()) }}
        </tr>
    </table>
    {{ void(textblock.end()) }}
{{ void(table.end()) }}

{{ space.widget({height: 15}) | raw }}

{{ void(table.begin({bgColor: '#FFFFFF', borderSize: 1})) }}
    {{ void(textblock.begin()) }}
    <table width="100%">
        <tr>
            {{ void(textblock.begin({row: false, width: '50%', align: 'center'})) }}
            Afhentnings tidspunkt<br />
            <strong>{{ timestampToDate(vm.startDate) }}</strong>
            {{ void(textblock.end()) }}

            {{ void(textblock.begin({row: false, width: '50%', align: 'center'})) }}
            Tilbageleverings tidspunkt<br />
            <strong>{{ timestampToDate(vm.endDate) }}</strong>
            {{ void(textblock.end()) }}
        </tr>
    </table>
    {{ void(textblock.end()) }}
{{ void(table.end()) }}

{{ void(table.begin({bgColor: '#EEEEEE'})) }}
    {{ space.widget({height: 15}) | raw }}

    {{ void(textblock.begin()) }}
    <strong>Betalingsoplysninger</strong>
    {{ void(textblock.end()) }}

    {{ space.widget({height: 15}) | raw }}

    {{ void(textblock.begin()) }}
        Betaling modtaget tor, {{ vm.payinDate }}
    {{ void(textblock.end()) }}

    {{ space.widget({height: 20}) | raw }}

{{ void(table.end()) }}

{{ void(table.begin({bgColor: '#FFFFFF', borderSize: 1, borderBottom: false})) }}
    {{ void(textblock.begin()) }}
    <table width="100%">
        <tr>
            {{ void(textblock.begin({row:false, width:'auto', align:'left'})) }}
            {{ vm.amountPerDay }} * {{ vm.rentingDays }} døgn
            {{ void(textblock.end()) }}
            {{ void(textblock.begin({row:false, width:'auto', align:'right'})) }}
            {{ vm.profileName }}
            {{ void(textblock.end()) }}
        </tr>
    </table>
    {{ void(textblock.end()) }}
{{ void(table.end()) }}

{{ void(table.begin({bgColor: '#FFFFFF', borderSize: 1, borderBottom: false})) }}
    {{ void(textblock.begin()) }}
    <table width="100%">
        <tr>
            {{ void(textblock.begin({row:false, width:'auto', align:'left'})) }}
            KidUp servicegebyr (inkl. moms)
            {{ void(textblock.end()) }}
            {{ void(textblock.begin({row:false, width:'auto', align:'right'})) }}
            {{ vm.amountServiceFee }}
            {{ void(textblock.end()) }}
        </tr>
    </table>
    {{ void(textblock.end()) }}
{{ void(table.end()) }}

{{ void(table.begin({bgColor: '#FFFFFF', borderSize: 1, borderBottom: false})) }}
    {{ void(textblock.begin()) }}
    <table width="100%">
        <tr>
            {{ void(textblock.begin({row:false, width:'auto', align:'left'})) }}
            Total
            {{ void(textblock.end()) }}
            {{ void(textblock.begin({row:false, width:'auto', align:'right'})) }}
            {{ vm.amountTotal }}
            {{ void(textblock.end()) }}
        </tr>
    </table>
    {{ void(textblock.end()) }}
{{ void(table.end()) }}

{{ void(table.begin({bgColor: '#FFFFFF', borderSize: 1, borderBottom: true})) }}
    {{ void(textblock.begin()) }}
    <table width="100%">
        <tr>
            {{ void(textblock.begin({row:false, width:'auto', align:'left'})) }}
            Balance
            {{ void(textblock.end()) }}
            {{ void(textblock.begin({row:false, width:'auto', align:'right'})) }}
            {{ vm.amountBalance }}
            {{ void(textblock.end()) }}
        </tr>
    </table>
    {{ void(textblock.end()) }}
{{ void(table.end()) }}