{{ void(table.begin({bgColor: '#EEEEEE'})) }}
    {{ greeter.widget({name: vm.profileName}) | raw }}

    {{ void(textblock.begin()) }}
        {{ vm.senderName }} sende dig en besked. Besvar {{ vm.senderName }} ved at svare direkte på denne e-mail
    {{ void(textblock.end()) }}

{{ void(table.end()) }}

{{ void(table.begin({bgColor: '#FFFFFF'})) }}
    {{ void(textblock.begin()) }}
        <i>{{ vm.message }}</i>

        {{ button.widget({
            content: 'Gå til samtale',
            url: chatUrl,
            wide: false
        }) | raw }}
    {{ void(textblock.end()) }}
{{ void(table.end()) }}